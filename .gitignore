# Rust
target/
*.pdb

# Node
node_modules/
.pnpm-store/

# Turborepo cache
.turbo/

# Build outputs (binaries are release artifacts, not committed)
dist/
*.tgz

# wasm-pack output
crates/fiducial-wasm/pkg/

# Local env
.env
.env.local
.dev.vars

# OS
.DS_Store
Thumbs.db

# svelte-check's generated type scratch. Regenerated on every `pnpm typecheck`,
# so committing it is committing a derived artifact — the rule this repository
# spends a whole phase enforcing everywhere else.
.svelte-check/

# Per-developer Claude Code permissions — machine-specific absolute paths.
.claude/settings.local.json
