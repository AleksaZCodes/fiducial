{
  "name": "@fiducial/ui-svelte",
  "version": "0.1.0",
  "description": "Fiducial component registry source — Svelte. Use `fid add component <name>` to copy components into your product.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build":     "echo 'Registry source — no build step. Use fid add component.'",
    "typecheck": "echo 'ui-svelte: .svelte files checked by svelte-check at build time'"
  },
  "peerDependencies": {
    "svelte": ">=5"
  },
  "dependencies": {
    "@fiducial/tokens":   "workspace:*",
    "@fiducial/headless": "workspace:*"
  },
  "devDependencies": {
    "svelte":      "^5.0.0",
    "typescript":  "^7.0.0"
  }
}
