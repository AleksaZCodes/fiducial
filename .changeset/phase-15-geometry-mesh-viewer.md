---
"@fiducial/viewer3d-react": minor
"@fiducial/board-schema": minor
---

Phase 15: 3D geometry pipeline — `fiducial-geometry` + `fiducial-mesh` Rust crates with `no_std` geometry primitives, binary STL and glTF 2.0 GLB export; `@fiducial/viewer3d-react` React component loading GLB via Three.js + GLTFLoader + OrbitControls; FDM/Resin/CNC tolerance profiles.
