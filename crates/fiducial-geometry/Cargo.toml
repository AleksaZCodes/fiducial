[package]
name        = "fiducial-geometry"
description = "no_std geometry for Fiducial: points, tolerance profiles, connector envelopes, and ear-clipping triangulation of polygons with holes."
version.workspace     = true
edition.workspace     = true
license.workspace     = true
repository.workspace  = true
homepage.workspace    = true
authors.workspace     = true
rust-version.workspace = true
keywords    = ["no-std", "geometry", "cad", "tolerance", "fiducial"]
categories  = ["no-std", "science"]

[lib]
name = "fiducial_geometry"

[features]
default = []
alloc   = []
std     = ["alloc"]

[dependencies]
libm = "0.2"
